export function Headline({ children }) {
  return (
    <span
      style={{
        fontSize: '36px',
        margin: '25px',
        marginLeft: '12.5px',
      }}
    >
      {children}{' '}
    </span>
  )
}
